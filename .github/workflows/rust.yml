name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  # Build and upload ph
  build-ph:
    name: zpr
    uses: org-zpr/zpr-dev-tools/.github/workflows/rust-build-test.yml@v1.1
    with:
      name: 'zpr'
    secrets: inherit

